 # if [ "$(tty)" = "/dev/tty1" ] && [ -n "$PS1" ]; then
 #   start-hyprland
 # elif [ "$(tty)" = "/dev/tty2" ] && [ -n "$PS1" ]; then
 #   niri
 # elif [ "$(tty)" = "/dev/tty3" ] && [ -n "$PS1" ]; then
 #   mango
 # fi
